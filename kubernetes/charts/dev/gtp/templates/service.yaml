apiVersion: v1
kind: Service
metadata:
  name: {{ include "gtp.fullname" . }}
  labels:
    {{- include "gtp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gtp.selectorLabels" . | nindent 4 }}
