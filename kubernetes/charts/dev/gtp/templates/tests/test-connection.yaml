apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gtp.fullname" . }}-test-connection"
  labels:
    {{- include "gtp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gtp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
